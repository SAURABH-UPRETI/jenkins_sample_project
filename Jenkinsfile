pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                echo 'Cloning GitHub repo...'
            }
        }

        stage('Build') {
            steps {
                echo 'Running build...'
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests...'
            }
        }
    }
}

